project:
  namespace: default
  app_name: abtesting-ui
  autoscaling:
    enabled: true
    default_replica: 1
    minReplicas: 1
    maxReplicas: 3
  service_port: 5000
  target_port: 8080
  image:
    name: abtesting-ui
    containerPort: 8080
  resources:
    limits:
      cpu: 2000m
      memory: 4Gi
    requests:
      cpu: 1200m
      memory: 2Gi
